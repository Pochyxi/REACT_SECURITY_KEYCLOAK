import {createTheme} from "@mui/material/styles";

const CircularWhite = createTheme({
    palette: {
        secondary: {
            main: '#ffffff',
        },
    },
});

export default CircularWhite;